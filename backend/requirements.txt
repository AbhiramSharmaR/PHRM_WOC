# --- Django Backend ---
django==4.2
djangorestframework==3.14.0
django-cors-headers==4.2.0

# --- MongoDB Drivers ---
pymongo==4.6.1
dnspython==2.5.0
motor==3.3.2    # async MongoDB (better than djongo)

# --- Authentication & Security ---
PyJWT==2.8.0
cryptography==42.0.5          # AES, secure hashes
pycryptodome==3.20.0          # AES-256 implementation
bcrypt==4.1.2

# --- AI/ML Frameworks ---
torch==2.3.0
torchvision==0.18.0
torchaudio==2.3.0
numpy==1.26.4
pandas==2.2.1
scikit-learn==1.4.1

# --- NLP / Transformers ---
transformers==4.38.2
sentencepiece==0.2.0
accelerate==0.27.2

# --- Explainability (Optional but useful for insights) ---
shap==0.45.0

# --- Blockchain Integration ---
web3==6.15.1

# --- APIs / Integrations / IoT ---
requests==2.31.0
google-api-python-client==2.119.0   # Google Fit API
python-dotenv==1.0.1

# --- Async Task Scheduling (Reminders / Behavior Engine) ---
APScheduler==3.10.4
celery==5.3.6
redis==5.0.2

# --- Validation / Utils ---
pydantic==2.6.3
uvicorn==0.29.0
fastapi==0.110.0         # Useful for submodules (optional utility API)

# --- Development Tools ---
black==24.2.0
flake8==7.0.0
pytest==8.1.0
pytest-django==4.8.0
